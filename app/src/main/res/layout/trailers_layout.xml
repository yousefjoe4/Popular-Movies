<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <View
        android:id="@+id/upper_line"
        style="@style/line_style"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toTopOf="parent" />

   <!-- <TextView
        android:id="@+id/tv_trailers"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="@strtrailer1lers"
        android:textSize="25sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="@+id/upper_line"
        app:layout_constraintTop_toTopOf="@id/upper_line" />

    <View
        android:id="@+id/lower_line"
        style="@style/line_style"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toBottomOf="@id/tv_trailers" />
-->
    <ImageView
        android:id="@+id/ic_play_trailer1"
        style="@style/icon_play_style"
        android:layout_marginTop="8dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="@id/upper_line" />

    <TextView
        android:id="@+id/tv_trailer1"
        style="@style/trailers_items_style"
        android:text="@string/trailer1"
        app:layout_constraintBottom_toBottomOf="@id/ic_play_trailer1"
        app:layout_constraintLeft_toRightOf="@id/ic_play_trailer1"
        app:layout_constraintTop_toTopOf="@id/ic_play_trailer1" />

    <ImageView
        android:id="@+id/ic_play_trailer2"
        style="@style/icon_play_style"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/ic_play_trailer1" />

    <TextView
        android:id="@+id/tv_trailer2"
        style="@style/trailers_items_style"
        android:text="@string/trailer2"
        app:layout_constraintBottom_toBottomOf="@id/ic_play_trailer2"
        app:layout_constraintLeft_toRightOf="@id/ic_play_trailer2"
        app:layout_constraintTop_toTopOf="@id/ic_play_trailer2" />

</android.support.constraint.ConstraintLayout>